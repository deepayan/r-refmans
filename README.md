# r-refmans

Steps should heve been documented, but were not.

Trying now:

* Run `./mirror-cran.sh` to update content

* Generate HTML refmans: `generate-html.R` has many non-default
  options, so use `generate-html-vanilla.R` instead.

* 
